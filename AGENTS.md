# AGENTS.md

本仓是 `gpt` 业务仓。

- 破坏性迁移按目标职责直接演进。
- 公共或跨服务契约以 `proto/` 为源头。
- 生成代码输出到各服务本地 `pb/` 或 Python `_pb2.py` 文件，提交前保持忽略。
- Go 服务优先通过 `./scripts/generate-proto.sh` 生成契约代码后做编译检查。
- 本仓不维护测试文件、测试目录、测试夹具、覆盖率报告或 CI/CD 配置。
- 运行时配置来自环境变量、配置文件或部署配置。
- secret、token、cookie、验证码、支付凭据、代理凭据和会话材料按敏感数据处理。
